int width  = 1920;
int height = 1080;
int channels = 3;
int data_size = 4;
int batch_size = 8;

for (int n = 0; n < batch_size; n++)
{
    for (int c = 0; c < channels; c++)
    {
        for (int x = 0; x < height*width; x++)
        {
            out = data_size*(c*(height*width) + x*channels + n*(height*width*c));           
        }
    }
}


